name: Workflow commands

on:
  push:
    branches:
      - main

jobs:
  workflow-job:
    runs-on: ubuntu-latest
    steps:
      - name: group logging
        run: |
          echo "::group::My group message"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"